/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findFavoritesPda } from "../pdas";
import { FAVORITES_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SET_FAVORITES_DISCRIMINATOR = new Uint8Array([
  211, 137, 87, 135, 161, 224, 187, 120,
]);

export function getSetFavoritesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_FAVORITES_DISCRIMINATOR,
  );
}

export type SetFavoritesInstruction<
  TProgram extends string = typeof FAVORITES_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountFavorites extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountFavorites extends string
        ? WritableAccount<TAccountFavorites>
        : TAccountFavorites,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetFavoritesInstructionData = {
  discriminator: ReadonlyUint8Array;
  number: bigint;
  color: string;
};

export type SetFavoritesInstructionDataArgs = {
  number: number | bigint;
  color: string;
};

export function getSetFavoritesInstructionDataEncoder(): Encoder<SetFavoritesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["number", getU64Encoder()],
      ["color", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: SET_FAVORITES_DISCRIMINATOR }),
  );
}

export function getSetFavoritesInstructionDataDecoder(): Decoder<SetFavoritesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["number", getU64Decoder()],
    ["color", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getSetFavoritesInstructionDataCodec(): Codec<
  SetFavoritesInstructionDataArgs,
  SetFavoritesInstructionData
> {
  return combineCodec(
    getSetFavoritesInstructionDataEncoder(),
    getSetFavoritesInstructionDataDecoder(),
  );
}

export type SetFavoritesAsyncInput<
  TAccountUser extends string = string,
  TAccountFavorites extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  favorites?: Address<TAccountFavorites>;
  systemProgram?: Address<TAccountSystemProgram>;
  number: SetFavoritesInstructionDataArgs["number"];
  color: SetFavoritesInstructionDataArgs["color"];
};

export async function getSetFavoritesInstructionAsync<
  TAccountUser extends string,
  TAccountFavorites extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FAVORITES_PROGRAM_ADDRESS,
>(
  input: SetFavoritesAsyncInput<
    TAccountUser,
    TAccountFavorites,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetFavoritesInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountFavorites,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAVORITES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    favorites: { value: input.favorites ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.favorites.value) {
    accounts.favorites.value = await findFavoritesPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.favorites),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetFavoritesInstructionDataEncoder().encode(
      args as SetFavoritesInstructionDataArgs,
    ),
    programAddress,
  } as SetFavoritesInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountFavorites,
    TAccountSystemProgram
  >);
}

export type SetFavoritesInput<
  TAccountUser extends string = string,
  TAccountFavorites extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  favorites: Address<TAccountFavorites>;
  systemProgram?: Address<TAccountSystemProgram>;
  number: SetFavoritesInstructionDataArgs["number"];
  color: SetFavoritesInstructionDataArgs["color"];
};

export function getSetFavoritesInstruction<
  TAccountUser extends string,
  TAccountFavorites extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FAVORITES_PROGRAM_ADDRESS,
>(
  input: SetFavoritesInput<
    TAccountUser,
    TAccountFavorites,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetFavoritesInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountFavorites,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FAVORITES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    favorites: { value: input.favorites ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.favorites),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetFavoritesInstructionDataEncoder().encode(
      args as SetFavoritesInstructionDataArgs,
    ),
    programAddress,
  } as SetFavoritesInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountFavorites,
    TAccountSystemProgram
  >);
}

export type ParsedSetFavoritesInstruction<
  TProgram extends string = typeof FAVORITES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    favorites: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: SetFavoritesInstructionData;
};

export function parseSetFavoritesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetFavoritesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      favorites: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetFavoritesInstructionDataDecoder().decode(instruction.data),
  };
}
